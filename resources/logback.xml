<?xml version="1.0" encoding="UTF-8"?>
<configuration>
	<!--Аппендеры -->

	<!--Консольный аппендер -->
	<appender name="stdout" class="ch.qos.logback.core.ConsoleAppender">
		<!-- filter class="ch.qos.logback.classic.filter.LevelFilter">
	        <level>INFO</level>
	        <onMatch>ACCEPT</onMatch>
	        <onMismatch>DENY</onMismatch>
		</filter-->
		<encoder>
			<Pattern>%d{HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n
			</Pattern>
		</encoder>
	</appender>

	<!--Файловый аппендер -->
	<appender name="file" class="ch.qos.logback.core.FileAppender">
		<file>log.log</file>
		<append>true</append>
		<!-- filter class="ch.qos.logback.classic.filter.LevelFilter">
	        <level>DEBUG</level>
	        <onMatch>ACCEPT</onMatch>
	        <onMismatch>DENY</onMismatch>
		</filter-->
		<encoder>
			<Pattern>%date %level [%thread] %logger{10} [%file:%line] %msg%n
			</Pattern>
		</encoder>
	</appender>

	<!-- Логгеры -->
	<!-- Корневой логгер -->
	<root level="info">
		<appender-ref ref="file" />
	</root>

<!-- root level="OFF">
</root-->

	<!-- Логгер нашего класса >
	<logger name="AccImporter.service.src" additivity="false">
		<appender-ref ref="stdout" />
	</logger>

	<logger name="AccImporter.service.src" level="DEBUG" additivity="true">
		<appender-ref ref="file" />
	</logger>

	<logger name="T4jClient.network.src" additivity="false">
		<appender-ref ref="stdout" />
	</logger>

	<logger name="T4jClient.network.src" level="DEBUG" additivity="true">
		<appender-ref ref="file" />
	</logger>

	<logger name="OAuthPasswordAuthenticator.network.src" additivity="false">
		<appender-ref ref="stdout" />
	</logger>

	<logger name="OAuthPasswordAuthenticator.network.src" level="DEBUG" additivity="true">
		<appender-ref ref="file" />
	</logger-->

</configuration>